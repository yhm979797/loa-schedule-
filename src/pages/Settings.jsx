import { useCharacters } from "../context/CharacterContext";
import { firebaseEnabled } from "../lib/firebase";
export default function Settings(){ const {characters}=useCharacters(); const backup=()=>{const blob=new Blob([JSON.stringify(characters,null,2)],{type:"application/json"});const a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download="loamate-backup.json";a.click();URL.revokeObjectURL(a.href)}; return <section><div className="section-title"><div><p className="eyebrow">SETTINGS</p><h2>설정</h2></div></div><div className="panel info-list"><div><span>Google 로그인</span><b>제거됨</b></div><div><span>캐릭터 저장</span><b>브라우저 LocalStorage</b></div><div><span>공용 공유 기능</span><b>{firebaseEnabled?"사용 가능":"Firebase 설정 필요"}</b></div><div><span>버전</span><b>3.0.0</b></div></div><button className="secondary full" onClick={backup}>내 레이드표 백업 다운로드</button></section> }
